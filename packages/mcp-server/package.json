{
  "name": "pgcloud-mcp",
  "version": "0.1.0",
  "description": "MCP server that lets Claude Code, Cursor and other agents use pgcloud through an agent token with a spending cap",
  "license": "MIT",
  "type": "module",
  "bin": { "pgcloud-mcp": "dist/index.js" },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "dev": "tsx src/index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.1",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  },
  "engines": { "node": ">=20" }
}
